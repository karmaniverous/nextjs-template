# These environment variables are only populated in the environment referenced in the file name.
# A version of this file should exist for every ENV supported by the project, named .env.ENV

# By default, environment variables are only available on the server side.
# Prefix a variable with NEXT_PUBLIC_ to expose it to the browser.
#
# Learn more about how this template handles environment variables at https://karmanivero.us/blog/nextjs-template/#environment-variables

# Site name elements for default META titles & menu header.
SITE_ENV_TOKEN=(test)

# Show/hide coming soon page.
COMING_SOON=0

# Next Auth canonical URL.
NEXTAUTH_URL=nextjs-template-test.karmanivero.us

# Next Auth provider settings.
NEXTAUTH_COGNITO_CLIENT_ID=64rvjrua47r2542qmgmekgjkii
NEXTAUTH_COGNITO_DOMAIN=aws-api-template-auth-v0-test.karmanivero.us
NEXTAUTH_COGNITO_REGION=us-east-1
NEXTAUTH_COGNITO_USER_POOL_ID=us-east-1_iCYWOFEnu

# AWS API base URL.
AWS_API_BASE_URL=https://aws-api-template.karmanivero.us/v0-test

# Log level.
LOG_LEVEL=debug